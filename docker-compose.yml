version: '3'
services:
  app:
    container_name: download-test
    restart: always
    build: .
    volumes:
      - ./:/home/debian/test
      - /src/:/home/debian/test
    tty: true
    ports:
      - '4040:4040'